[**CIA Compliance Manager — UML Diagrams v1.1.47**](../../../README.md)

***

[CIA Compliance Manager — UML Diagrams](../../../modules.md) / [constants/keyboardShortcuts](../README.md) / KEY\_DISPLAY\_NAMES

# Variable: KEY\_DISPLAY\_NAMES

> `const` **KEY\_DISPLAY\_NAMES**: `object`

Defined in: [constants/keyboardShortcuts.ts:141](https://github.com/Hack23/cia-compliance-manager/blob/0a914ff8809ea300c13e0a51b2ef582dc0e7a4a4/src/constants/keyboardShortcuts.ts#L141)

Key display names for different platforms

## Type Declaration

### ctrl

> `readonly` **ctrl**: `object`

#### ctrl.windows

> `readonly` **windows**: `"Ctrl"` = `'Ctrl'`

#### ctrl.mac

> `readonly` **mac**: `"⌘"` = `'⌘'`

#### ctrl.linux

> `readonly` **linux**: `"Ctrl"` = `'Ctrl'`

#### ctrl.unknown

> `readonly` **unknown**: `"Ctrl"` = `'Ctrl'`

### shift

> `readonly` **shift**: `object`

#### shift.windows

> `readonly` **windows**: `"Shift"` = `'Shift'`

#### shift.mac

> `readonly` **mac**: `"⇧"` = `'⇧'`

#### shift.linux

> `readonly` **linux**: `"Shift"` = `'Shift'`

#### shift.unknown

> `readonly` **unknown**: `"Shift"` = `'Shift'`

### alt

> `readonly` **alt**: `object`

#### alt.windows

> `readonly` **windows**: `"Alt"` = `'Alt'`

#### alt.mac

> `readonly` **mac**: `"⌥"` = `'⌥'`

#### alt.linux

> `readonly` **linux**: `"Alt"` = `'Alt'`

#### alt.unknown

> `readonly` **unknown**: `"Alt"` = `'Alt'`

### meta

> `readonly` **meta**: `object`

#### meta.windows

> `readonly` **windows**: `"Win"` = `'Win'`

#### meta.mac

> `readonly` **mac**: `"⌘"` = `'⌘'`

#### meta.linux

> `readonly` **linux**: `"Meta"` = `'Meta'`

#### meta.unknown

> `readonly` **unknown**: `"Meta"` = `'Meta'`

### cmd

> `readonly` **cmd**: `object`

#### cmd.windows

> `readonly` **windows**: `"Ctrl"` = `'Ctrl'`

#### cmd.mac

> `readonly` **mac**: `"⌘"` = `'⌘'`

#### cmd.linux

> `readonly` **linux**: `"Ctrl"` = `'Ctrl'`

#### cmd.unknown

> `readonly` **unknown**: `"Ctrl"` = `'Ctrl'`

### enter

> `readonly` **enter**: `object`

#### enter.windows

> `readonly` **windows**: `"Enter"` = `'Enter'`

#### enter.mac

> `readonly` **mac**: `"↵"` = `'↵'`

#### enter.linux

> `readonly` **linux**: `"Enter"` = `'Enter'`

#### enter.unknown

> `readonly` **unknown**: `"Enter"` = `'Enter'`

### escape

> `readonly` **escape**: `object`

#### escape.windows

> `readonly` **windows**: `"Esc"` = `'Esc'`

#### escape.mac

> `readonly` **mac**: `"Esc"` = `'Esc'`

#### escape.linux

> `readonly` **linux**: `"Esc"` = `'Esc'`

#### escape.unknown

> `readonly` **unknown**: `"Esc"` = `'Esc'`

### backspace

> `readonly` **backspace**: `object`

#### backspace.windows

> `readonly` **windows**: `"Backspace"` = `'Backspace'`

#### backspace.mac

> `readonly` **mac**: `"⌫"` = `'⌫'`

#### backspace.linux

> `readonly` **linux**: `"Backspace"` = `'Backspace'`

#### backspace.unknown

> `readonly` **unknown**: `"Backspace"` = `'Backspace'`

### tab

> `readonly` **tab**: `object`

#### tab.windows

> `readonly` **windows**: `"Tab"` = `'Tab'`

#### tab.mac

> `readonly` **mac**: `"⇥"` = `'⇥'`

#### tab.linux

> `readonly` **linux**: `"Tab"` = `'Tab'`

#### tab.unknown

> `readonly` **unknown**: `"Tab"` = `'Tab'`

### arrowup

> `readonly` **arrowup**: `object`

#### arrowup.windows

> `readonly` **windows**: `"↑"` = `'↑'`

#### arrowup.mac

> `readonly` **mac**: `"↑"` = `'↑'`

#### arrowup.linux

> `readonly` **linux**: `"↑"` = `'↑'`

#### arrowup.unknown

> `readonly` **unknown**: `"↑"` = `'↑'`

### arrowdown

> `readonly` **arrowdown**: `object`

#### arrowdown.windows

> `readonly` **windows**: `"↓"` = `'↓'`

#### arrowdown.mac

> `readonly` **mac**: `"↓"` = `'↓'`

#### arrowdown.linux

> `readonly` **linux**: `"↓"` = `'↓'`

#### arrowdown.unknown

> `readonly` **unknown**: `"↓"` = `'↓'`

### arrowleft

> `readonly` **arrowleft**: `object`

#### arrowleft.windows

> `readonly` **windows**: `"←"` = `'←'`

#### arrowleft.mac

> `readonly` **mac**: `"←"` = `'←'`

#### arrowleft.linux

> `readonly` **linux**: `"←"` = `'←'`

#### arrowleft.unknown

> `readonly` **unknown**: `"←"` = `'←'`

### arrowright

> `readonly` **arrowright**: `object`

#### arrowright.windows

> `readonly` **windows**: `"→"` = `'→'`

#### arrowright.mac

> `readonly` **mac**: `"→"` = `'→'`

#### arrowright.linux

> `readonly` **linux**: `"→"` = `'→'`

#### arrowright.unknown

> `readonly` **unknown**: `"→"` = `'→'`
